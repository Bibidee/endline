export const KINDS=["API","SDK","MODEL","PACKAGE","PROTOCOL","SERVICE","OTHER"] as const;
export function validateSources(sources:string[]){return sources.length>=1&&sources.length<=3&&sources.every(s=>{try{const u=new URL(s);const h=u.hostname.toLowerCase().replace(/^\[|\]$/g,'');const privateV4=/^(10\.|127\.|192\.168\.|169\.254\.|172\.(1[6-9]|2\d|3[01])\.|0\.)/.test(h);const unsafeV6=h==='::1'||h==='::'||h.startsWith('fc')||h.startsWith('fd')||h.startsWith('fe8')||h.startsWith('fe9')||h.startsWith('fea')||h.startsWith('feb');return u.protocol==='https:'&&!u.username&&!u.password&&h!=='localhost'&&!h.endsWith('.local')&&!privateV4&&!unsafeV6&&s.length<=500}catch{return false}})}
